package Uebungen;

import static Uebungen.Merge.merge;

public class Merge {
    public static int merge(long[] ns, int i, Merger m) {
        m.merge();
        int b = 0;
        if (i < 0){
            return merge( ns[0] * ns[b], i, m);
            b++;
        }
        else if (i > ns.length-1){
            ns[]= new int ns[0];
            return ns[0];
            //leeres Array ausgeben
        }
        else{
            if()




        }

    }
}
